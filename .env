# VITE_API_URL unset — Vite proxy forwards /api/* to :3001

# Auth — direct backend URL for OAuth redirects
VITE_BACKEND_URL=http://localhost:3001

# Keycloak (disabled)
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=greater-agents
VITE_KEYCLOAK_CLIENT_ID=greater-agents-app
